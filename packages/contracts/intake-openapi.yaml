openapi: 3.1.0
info:
  title: Intake Service API
  version: 0.1.0
  x-domain: intake
  x-status: alpha
  x-visibility: public
  description: |
    REST API for managing public assistance applications. This is the minimal
    baseline — ApplicationMember, Income, Expense, and Asset sub-resources
    are added incrementally as the intake domain is built out.

    > **Status: Alpha** — Breaking changes expected.

  contact:
    name: API Support
    email: support@example.com

servers:
  - url: https://api.example.com/intake
    description: Production server
  - url: http://localhost:1080/intake
    description: Local development server

tags:
  - name: Applications
    description: Manage public assistance applications.
  - name: Documents
    description: Application document requirements and verification tracking.
  - name: Interviews
    description: SNAP interview obligation tracking.

x-events:
  application.created:
    type: org.codeforamerica.safety-net-blueprint.intake.application.created
    summary: Emitted when an application record is first created (auto-emitted on POST)
    payload:
      $ref: "#/components/schemas/ApplicationCreatedEvent"
  application.submitted:
    type: org.codeforamerica.safety-net-blueprint.intake.application.submitted
    summary: Emitted when an applicant formally submits their application; starts the regulatory clock
    payload:
      $ref: "#/components/schemas/ApplicationSubmittedEvent"
  application.opened:
    type: org.codeforamerica.safety-net-blueprint.intake.application.opened
    summary: Emitted when a caseworker begins active review of the application
    payload:
      $ref: "#/components/schemas/ApplicationOpenedEvent"
  application.withdrawn:
    type: org.codeforamerica.safety-net-blueprint.intake.application.withdrawn
    summary: Emitted when an application is withdrawn; triggers open task cancellation
    payload:
      $ref: "#/components/schemas/ApplicationWithdrawnEvent"
  application.closed:
    type: org.codeforamerica.safety-net-blueprint.intake.application.closed
    summary: Emitted when intake is complete and the application is ready for eligibility determination
    payload:
      $ref: "#/components/schemas/ApplicationClosedEvent"
  application.expedited_flagged:
    type: org.codeforamerica.safety-net-blueprint.intake.application.expedited_flagged
    summary: Emitted when an application is flagged for expedited processing
    payload:
      $ref: "#/components/schemas/ApplicationExpeditedFlaggedEvent"
  application.review_completed:
    type: org.codeforamerica.safety-net-blueprint.intake.application.review_completed
    summary: Emitted when a caseworker signals data collection is complete; triggers eligibility determination. No state change.
    payload:
      $ref: "#/components/schemas/ApplicationReviewCompletedEvent"
  application.updated:
    type: org.codeforamerica.safety-net-blueprint.intake.application.updated
    summary: Emitted when application fields are updated via PATCH (auto-emitted)
    payload:
      $ref: "#/components/schemas/ApplicationUpdatedEvent"
  application.deleted:
    type: org.codeforamerica.safety-net-blueprint.intake.application.deleted
    summary: Emitted when an application is deleted (auto-emitted on DELETE)
    payload:
      $ref: "#/components/schemas/ApplicationDeletedEvent"

paths:
  /applications:
    get:
      summary: List applications
      description: Retrieve a paginated list of applications.
      operationId: listApplications
      tags:
        - Applications
      parameters:
        - $ref: "./components/parameters.yaml#/SearchQueryParam"
        - $ref: "./components/parameters.yaml#/LimitParam"
        - $ref: "./components/parameters.yaml#/OffsetParam"
      responses:
        "200":
          description: A paginated collection of applications.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationList"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    post:
      summary: Create an application
      description: Create a new public assistance application in draft status.
      operationId: createApplication
      tags:
        - Applications
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationCreate"
      responses:
        "201":
          description: Application created successfully.
          headers:
            Location:
              description: URL of the newly created application resource.
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "422":
          $ref: "./components/responses.yaml#/UnprocessableEntity"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

  /applications/{applicationId}:
    parameters:
      - $ref: "#/components/parameters/ApplicationIdParam"

    get:
      summary: Get an application
      description: Retrieve a single application by identifier.
      operationId: getApplication
      tags:
        - Applications
      responses:
        "200":
          description: Application retrieved successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
              examples:
                ApplicationExample1:
                  $ref: "#/components/examples/ApplicationExample1"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    patch:
      summary: Update an application
      description: Apply partial updates to a draft or under-review application.
      operationId: updateApplication
      tags:
        - Applications
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationUpdate"
      responses:
        "200":
          description: Application updated successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Application"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "422":
          $ref: "./components/responses.yaml#/UnprocessableEntity"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    delete:
      summary: Delete an application
      description: Permanently remove a draft application record.
      operationId: deleteApplication
      tags:
        - Applications
      responses:
        "204":
          description: Application deleted successfully.
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

  /applications/{applicationId}/documents:
    parameters:
      - $ref: "#/components/parameters/ApplicationIdParam"

    get:
      summary: List application documents
      description: Retrieve document requirements for a specific application.
      operationId: listApplicationDocuments
      tags:
        - Documents
      parameters:
        - $ref: "./components/parameters.yaml#/SearchQueryParam"
        - $ref: "./components/parameters.yaml#/LimitParam"
        - $ref: "./components/parameters.yaml#/OffsetParam"
      responses:
        "200":
          description: A paginated list of application documents.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDocumentList"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    post:
      summary: Create an application document
      description: Create a document requirement scoped to this application. Under normal circumstances documents are created by the rules engine; this endpoint supports caseworker-initiated requests.
      operationId: createApplicationDocument
      tags:
        - Documents
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationDocumentCreate"
      responses:
        "201":
          description: Application document created successfully.
          headers:
            Location:
              description: URL of the newly created document resource.
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDocument"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "422":
          $ref: "./components/responses.yaml#/UnprocessableEntity"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

  /applications/{applicationId}/documents/{documentId}:
    parameters:
      - $ref: "#/components/parameters/ApplicationIdParam"
      - $ref: "#/components/parameters/DocumentIdParam"

    get:
      summary: Get an application document
      description: Retrieve a single document requirement by identifier.
      operationId: getApplicationDocument
      tags:
        - Documents
      responses:
        "200":
          description: Application document retrieved successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDocument"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    patch:
      summary: Update an application document
      description: Apply partial updates to an application document.
      operationId: updateApplicationDocument
      tags:
        - Documents
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationDocumentUpdate"
      responses:
        "200":
          description: Application document updated successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDocument"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "422":
          $ref: "./components/responses.yaml#/UnprocessableEntity"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    delete:
      summary: Delete an application document
      description: Remove a document requirement from this application.
      operationId: deleteApplicationDocument
      tags:
        - Documents
      responses:
        "204":
          description: Application document deleted successfully.
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

  /applications/{applicationId}/interview:
    parameters:
      - $ref: "#/components/parameters/ApplicationIdParam"

    get:
      summary: Get the interview
      description: Retrieve the SNAP interview obligation record for this application.
      operationId: getInterview
      tags:
        - Interviews
      responses:
        "200":
          description: Interview retrieved successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Interview"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

    patch:
      summary: Update the interview
      description: Apply partial updates to the interview record (e.g., set waiverGranted, record completedAt).
      operationId: updateInterview
      tags:
        - Interviews
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InterviewUpdate"
      responses:
        "200":
          description: Interview updated successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Interview"
        "400":
          $ref: "./components/responses.yaml#/BadRequest"
        "404":
          $ref: "./components/responses.yaml#/NotFound"
        "422":
          $ref: "./components/responses.yaml#/UnprocessableEntity"
        "500":
          $ref: "./components/responses.yaml#/InternalError"

components:
  parameters:
    ApplicationIdParam:
      name: applicationId
      in: path
      required: true
      description: Unique identifier of the application.
      schema:
        type: string
        format: uuid

    DocumentIdParam:
      name: documentId
      in: path
      required: true
      description: Unique identifier of the application document.
      schema:
        type: string
        format: uuid

  schemas:
    ApplicationWritable:
      type: object
      description: Client-writable fields for an application.
      properties:
        programs:
          type: array
          description: Programs the household is applying for. At least one required at submission.
          items:
            type: string
            enum:
              - snap
              - medicaid
              - tanf
        channel:
          type: string
          description: How the application was filed.
          enum:
            - online
            - in_person
            - phone
            - mail

    Application:
      allOf:
        - $ref: "#/components/schemas/ApplicationWritable"
        - type: object
          required:
            - id
            - status
            - createdAt
            - updatedAt
          properties:
            id:
              type: string
              format: uuid
              description: Unique identifier for the application.
              readOnly: true
            status:
              type: string
              description: Current lifecycle status of the application.
              readOnly: true
              enum:
                - draft
                - submitted
                - under_review
                - withdrawn
                - closed
            isExpedited:
              type:
                - boolean
                - "null"
              description: Whether the application qualifies for expedited processing (e.g., SNAP 7-day track). Set during caseworker expedited screening after submission. Null until screening is complete.
              readOnly: true
            submittedAt:
              type: string
              format: date-time
              description: When the application was formally submitted. Null until submitted.
              readOnly: true
              nullable: true
            withdrawnAt:
              type: string
              format: date-time
              description: When the application was withdrawn. Null unless withdrawn.
              readOnly: true
              nullable: true
            closedAt:
              type: string
              format: date-time
              description: When the intake phase closed. Null until closed.
              readOnly: true
              nullable: true
            createdAt:
              type: string
              format: date-time
              description: When the application record was created.
              readOnly: true
            updatedAt:
              type: string
              format: date-time
              description: When the application record was last modified.
              readOnly: true

    ApplicationCreate:
      allOf:
        - $ref: "#/components/schemas/ApplicationWritable"
        - type: object
          required:
            - programs

    ApplicationUpdate:
      allOf:
        - $ref: "#/components/schemas/ApplicationWritable"
        - type: object
          minProperties: 1

    ApplicationList:
      allOf:
        - $ref: "./components/pagination.yaml#/Pagination"
        - type: object
          required:
            - items
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/Application"

    # ---------------------------------------------------------------------------
    # ApplicationDocument
    # ---------------------------------------------------------------------------

    ApplicationDocumentWritable:
      type: object
      description: Client-writable fields for an application document requirement.
      properties:
        memberId:
          type: string
          format: uuid
          description: Member this document is required for. Null for household-level requirements (e.g., proof of residency).
          nullable: true
          x-relationship:
            resource: ApplicationMember
        category:
          type: string
          description: Type of documentation required.
          enum:
            - income
            - identity
            - residency
            - citizenship

    ApplicationDocument:
      allOf:
        - $ref: "#/components/schemas/ApplicationDocumentWritable"
        - type: object
          required:
            - id
            - applicationId
            - category
            - status
            - createdAt
            - updatedAt
          properties:
            id:
              type: string
              format: uuid
              readOnly: true
            applicationId:
              type: string
              format: uuid
              readOnly: true
              x-relationship:
                resource: Application
            status:
              type: string
              description: Current verification status of the document requirement.
              readOnly: true
              enum:
                - requested
                - verified
            createdAt:
              type: string
              format: date-time
              readOnly: true
            updatedAt:
              type: string
              format: date-time
              readOnly: true

    ApplicationDocumentCreate:
      allOf:
        - $ref: "#/components/schemas/ApplicationDocumentWritable"
        - type: object
          required:
            - category

    ApplicationDocumentUpdate:
      allOf:
        - $ref: "#/components/schemas/ApplicationDocumentWritable"
        - type: object
          minProperties: 1

    ApplicationDocumentList:
      allOf:
        - $ref: "./components/pagination.yaml#/Pagination"
        - type: object
          required:
            - items
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/ApplicationDocument"

    # ---------------------------------------------------------------------------
    # Interview
    # ---------------------------------------------------------------------------

    InterviewWritable:
      type: object
      description: Caseworker-writable fields for an interview obligation record.
      properties:
        waiverGranted:
          type:
            - boolean
            - "null"
          description: Whether the interview requirement has been waived for this applicant.
        waiverReason:
          type: string
          description: Reason the interview was waived. Required when waiverGranted is true.
          nullable: true
        completedAt:
          type: string
          format: date-time
          description: When the caseworker attested the interview obligation was satisfied. Null until completed.
          nullable: true

    Interview:
      allOf:
        - $ref: "#/components/schemas/InterviewWritable"
        - type: object
          required:
            - id
            - applicationId
            - appointments
            - createdAt
            - updatedAt
          properties:
            id:
              type: string
              format: uuid
              readOnly: true
            applicationId:
              type: string
              format: uuid
              readOnly: true
              x-relationship:
                resource: Application
            appointments:
              type: array
              description: Appointment IDs from the scheduling domain accumulated for this interview obligation. Populated by the rules engine when scheduling.appointment.scheduled fires.
              readOnly: true
              items:
                type: string
                format: uuid
                x-relationship:
                  resource: External
            createdAt:
              type: string
              format: date-time
              readOnly: true
            updatedAt:
              type: string
              format: date-time
              readOnly: true

    InterviewUpdate:
      allOf:
        - $ref: "#/components/schemas/InterviewWritable"
        - type: object
          minProperties: 1

    # Event payload schemas — referenced by x-events section above

    ApplicationReviewCompletedEvent:
      type: object
      description: Payload for application.review_completed — signals data collection is complete; triggers eligibility determination.
      required:
        - reviewCompletedAt
      properties:
        reviewCompletedAt:
          type: string
          format: date-time
          description: When the caseworker signaled review was complete.

    ApplicationClosedEvent:
      type: object
      description: Payload for application.closed — signals intake is complete.
      required:
        - closedAt
      properties:
        closedAt:
          type: string
          format: date-time
          description: When the intake phase closed.

    ApplicationCreatedEvent:
      description: Snapshot of the newly created application resource.
      $ref: "#/components/schemas/Application"

    ApplicationDeletedEvent:
      type: object
      description: Payload for application.deleted — auto-emitted when an application record is permanently removed.
      required:
        - id
      properties:
        id:
          type: string
          format: uuid
          description: Identifier of the deleted application.

    ApplicationExpeditedFlaggedEvent:
      type: object
      description: Payload for application.expedited_flagged — triggers escalation to expedited SLA track.
      required:
        - flaggedAt
      properties:
        flaggedAt:
          type: string
          format: date-time
          description: When the expedited flag was set.

    ApplicationOpenedEvent:
      type: object
      description: Payload for application.opened — signals caseworker has begun active review.
      required:
        - openedAt
        - programs
      properties:
        openedAt:
          type: string
          format: date-time
          description: When the application moved to under_review.
        programs:
          type: array
          description: Programs the household is applying for. Included so downstream systems can fan out per-program work after intake screening (see Decision 15).
          items:
            type: string
            enum:
              - snap
              - medicaid
              - tanf

    ApplicationSubmittedEvent:
      type: object
      description: Payload for application.submitted — starts the regulatory clock; triggers program-type-aware routing (see Decision 15).
      required:
        - submittedAt
        - programs
      properties:
        submittedAt:
          type: string
          format: date-time
          description: When the application was formally submitted.
        programs:
          type: array
          description: Programs the household is applying for. Consumers route per program type — not all programs generate caseworker tasks at submission.
          items:
            type: string
            enum:
              - snap
              - medicaid
              - tanf
        channel:
          type: string
          description: How the application was filed.
          enum:
            - online
            - in_person
            - phone
            - mail
        isExpedited:
          type:
            - boolean
            - "null"
          description: Whether expedited screening has already determined the application qualifies. Null if not yet screened.

    ApplicationUpdatedEvent:
      type: object
      description: Payload for application.updated — auto-emitted on PATCH; contains only the fields that changed.
      additionalProperties: true

    ApplicationWithdrawnEvent:
      type: object
      description: Payload for application.withdrawn — triggers open task cancellation and withdrawal notice.
      required:
        - withdrawnAt
      properties:
        withdrawnAt:
          type: string
          format: date-time
          description: When the application was withdrawn.
        reason:
          type: string
          description: Why the application was withdrawn.

  examples:
    ApplicationExample1:
      value:
        id: 00000004-0000-4000-8000-000000000001
        status: submitted
        programs:
          - snap
          - medicaid
        channel: online
        isExpedited: false
        submittedAt: "2026-04-01T09:15:00Z"
        withdrawnAt: null
        closedAt: null
        createdAt: "2026-04-01T09:00:00Z"
        updatedAt: "2026-04-01T09:15:00Z"
